// Appended to the accessible name of an icon link that opens in a new tab.
// The mark itself is decorative (`alt=""`), so without this the cue would
// reach sighted users only - and only those who recognise the convention.
export const NEW_TAB_LABEL = " (opens in a new tab)";
